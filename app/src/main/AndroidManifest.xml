<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chicagoxleftovers">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChicagoxLeftovers">
        <activity
            android:name=".ListMakanan"
            android:exported="false" />
        <activity
            android:name=".HalamanToko"
            android:exported="false" />
        <activity
            android:name=".MetodePembayaran"
            android:exported="false" />
        <activity
            android:name=".Pengiriman"
            android:exported="false" />
        <activity
            android:name=".NotifBerhasil"
            android:exported="false"
            android:parentActivityName=".MainActivity">

            <!-- Khusus untuk android API < 15 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>